/** Auto-reject if modelled slop exceeds this (0–1). */
export const VISUAL_SLOP_REJECT_THRESHOLD = 0.72;
/** Auto-reject if realism score falls below this (0–1). */
export const VISUAL_REALISM_REJECT_THRESHOLD = 0.38;
/** One Generate click produces this many variants (inclusive bounds). */
export const VISUAL_VARIANTS_PER_RUN_MIN = 6;
export const VISUAL_VARIANTS_PER_RUN_MAX = 12;
